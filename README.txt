This work implements two classes in c++: Vector and Matrix.

Several constructors available for both classes. 
Addition, scalar multiplication and multiplication are implemented.

To run the test, navigate into build and type:

cmake ../

This will generate the Makefile. Then type:

make 

build the executable "matrix", type:

./matrix

to run the test.
